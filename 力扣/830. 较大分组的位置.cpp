#include <iostream>
#include <vector>
#include <string>
using namespace std;
/*我们称所有包含大于或等于三个连续字符的分组为 较大分组 。*/
class Solution {
public:
    vector<vector<int> > largeGroupPositions(string s) {
        vector<vector<int> > result;
        

        return result;
    }
};